# watchOS Client Surface

This track is implemented in the shared Apple-native workspace (`clients/apple`).

- App entry: `clients/apple/watchOS/KaigiWatchOSApp.swift`
- Shared runtime/UI/model code: `clients/apple/Shared/`
- Smoke scenario: `APPLE-BUILD-006` via `scripts/run_native_apple_smoke.sh --platform watchos`
